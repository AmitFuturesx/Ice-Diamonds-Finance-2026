# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY=""

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL=""

# Supabase Configurations for Secure Live DB & Authentication (Production mode)
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# Secure Mock Credentials variables (for local/private staging bypass)
VITE_STAGING_USER_1="amitshop23@gmail.com"
VITE_STAGING_USER_2="DNE@hasharon.diamonds"
VITE_STAGING_PASSWORD="123456"
VITE_HIDE_DEMO_NOTICE="false" # Set to "true" to fully hide the helpful credentials hint box on the login view
